import { StyleSheet, View } from "react-native";
import React from "react";
import ScreenWrapper from "@screens/ScreenWrapper";
import { AppText } from "@atoms";

export const Cards = () => {
  return (
    <ScreenWrapper>
      <AppText>Cards</AppText>
    </ScreenWrapper>
  );
};

const styles = StyleSheet.create({});

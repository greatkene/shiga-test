import { StyleSheet, View } from "react-native";
import React from "react";
import { AppText } from "@atoms";
import ScreenWrapper from "@screens/ScreenWrapper";

export const TopUp = () => {
  return (
    <ScreenWrapper>
      <AppText>TopUp</AppText>
    </ScreenWrapper>
  );
};

const styles = StyleSheet.create({});
